{
  "name": "node-docker-debug",
  "version": "1.0.0",
  "description": "Develop and debug node.js inside docker container",
  "main": "./src/index.js",
  "scripts": {
    "debug": "docker-compose -f docker-compose.prod.yml -f docker-compose.dev.yml --build -d up",
    "prod": "docker-compose -f docker-compose.prod.yml --buil -d up",
    "start": "node ./src/index.js"
  },
  "keywords": [
    "node",
    "docker"
  ],
  "author": "Nazar Matviyiv",
  "license": "ISC",
  "dependencies": {
    "micro": "^7.0.3"
  }
}
